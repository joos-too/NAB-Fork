# Copy to `config/simple_stats.env` and edit values to tune the detectors.
# Lines are KEY=VALUE, comments start with '#'.

# -----------------------
# Z-Score detector tuning
# -----------------------
# Suggested starting point: larger window + slightly lower center.
NAB_ZSCORE_WINDOW=250
NAB_ZSCORE_THRESHOLD=2.5
NAB_ZSCORE_SCALE=0.7
NAB_ZSCORE_MIN_STD=1e-6

# -------------------
# EWMA detector tuning
# -------------------
# Suggested starting point: smoother EWMA.
NAB_EWMA_ALPHA=0.1
NAB_EWMA_THRESHOLD=3.0
NAB_EWMA_SCALE=0.8
NAB_EWMA_MIN_STD=1e-6

# --------------------------------
# Adaptive threshold detector tuning
# --------------------------------
NAB_ADAPTIVE_WINDOW=100
NAB_ADAPTIVE_SENSITIVITY=2.0
NAB_ADAPTIVE_SCALE=0.6
NAB_ADAPTIVE_MIN_DEV=1e-6
